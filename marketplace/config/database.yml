default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: proven_marketplace_development

test:
  <<: *default
  database: proven_marketplace_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
